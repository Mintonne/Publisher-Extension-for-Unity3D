{
  "devDependencies": {
    "gulp": "^4.0.0",
    "gulp-babel-minify": "^0.5.0",
    "gulp-clean-css": "^4.0.0",
    "gulp-concat": "^2.6.1",
    "gulp-rename": "^1.4.0",
    "gulp-rimraf": "^0.2.2",
    "gulp-strip-code": "^0.1.4",
    "gulp-zip": "^4.2.0",
    "streamqueue": "^1.1.2"
  },
  "scripts": {
    "compile-chrome": "npx gulp compile-chrome",
    "compile-firefox": "npx gulp compile-firefox",
    "build-chrome": "npx gulp compile-chrome && npx gulp build-chrome",
    "build-firefox": "npx gulp compile-firefox && npx gulp build-firefox"
  }
}
